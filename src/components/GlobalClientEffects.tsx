"use client";

/**
 * GlobalClientEffects
 * Efectos globales del cliente que se ejecutan en el layout raíz.
 * Actualmente sin efectos activos — espacio reservado para futuros
 * listeners de analytics, PWA, o notificaciones push.
 */
export default function GlobalClientEffects() {
  return null;
}
